import Buttons from './BstageComponents/Buttons'
import Stats from './BstageComponents/Stats'

const Bstage = () => {
    return (
        <div style={{clear:'both'}}>
            <Buttons/>
            <Stats/>
        </div>
    )
}

export default Bstage
